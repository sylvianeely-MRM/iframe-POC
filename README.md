# GM Rewards POC

Basic static HTML project scaffold.

## Files

- `index.html`: main page
- `styles.css`: page styles
- `script.js`: basic interaction handler

## Run

Open `index.html` directly in a browser, or serve the folder with a simple static server.
